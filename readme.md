# Inicializar y correr archivos

Primero, instalar dependencias

``` npm i ```

En el directorio de cada tema, a la altura de cada app.ts, correr

``` npx ts-node app.ts ```

para ver el resultado de la ejecución del programa.
